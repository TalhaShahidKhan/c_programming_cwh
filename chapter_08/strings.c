#include <stdio.h>
#include<string.h>

int main(){
    // char st[4]={'d','d','f','\0'};
    // for (int i = 0; i < 3; i++){
    //     printf("%c\n",st[i]);
    // }
    // printf("%s",st);
    char name[23];
    // scanf("%s",name);
    // printf("%s",name);
    fgets(name,sizeof(name),stdin);
    puts(name);
    printf("dlfjad");
    return 0;

}